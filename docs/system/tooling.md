# Тулчейн: версии, команды, что есть и чего нет

Инструменты по памяти не настраиваем — сверяемся отсюда. Версии обновились —
патчим этот файл тем же коммитом.

## Стек и версии

Источник — `package.json` (прямые зависимости закреплены ТОЧНЫМИ версиями,
без `^` и `~` — это осознанно, обновление всегда явное).

| Слой | Что | Версия |
|---|---|---|
| Среда | Node | `22.x` (`engines.node`); **на машине прораба стоит v24.18.0 — расхождение** |
| Язык | TypeScript | 5.9.3 |
| Сборка | Vite | 7.3.6 |
| Плагин | `@vitejs/plugin-react` | 5.2.0 |
| UI | React / React DOM | 19.2.8 |
| Роутинг | `react-router-dom` | 7.18.2 |
| Данные | `@supabase/supabase-js` | 2.50.2 |
| Иконки | `lucide-react` | 0.468.0 |
| Типы | `@types/react` / `@types/react-dom` | 19.2.18 / 19.2.4 |

Хостинг — Vercel. База, Auth и Data API — Supabase (Postgres).

**Расхождение версий Node.** `engines.node` требует 22.x, локально стоит 24.18.0.
Vercel соберёт на 22.x, локальная сборка — на 24.x: «у меня собралось» не доказывает,
что соберётся на проде. Решение (зафиксировать 22 через nvm или поднять `engines`) —
за прорабом, пока это открытый долг.

## Команды

| Команда | Что делает |
|---|---|
| `npm install` | поставить зависимости (в свежем клоне `node_modules` нет) |
| `npm run dev` | Vite dev-сервер; требует заполненного `.env` |
| `npm run build` | `tsc -b && vite build` — основной гейт |
| `npm run build:vercel` | то же с `--mode production` |
| `npm run preview` | локальный просмотр собранного |
| `npm run images:equipment` | пересборка фото оборудования; на macOS требует `sips` и `cwebp` |
| `npx tsc -b` | быстрая проверка типов без сборки; кэширует — `--force` сбрасывает |

## Гейты приёмки

Порядок строгий (он же в `/checkpoint`):

```bash
npx tsc -b
npm run build
git diff --check
```

## Чего в проекте НЕТ

Это важнее списка того, что есть: агент не должен ссылаться на несуществующее.

- **Линтера нет.** `.eslintrc.json` в корне — конфиг от прошлого, Vue-стека;
  ни `eslint`, ни `eslint-plugin-vue` не установлены, скрипта `lint` нет.
- **Тестов нет.** `vitest.config.js` — оттуда же (`@vitejs/plugin-vue`, jsdom);
  ни `vitest`, ни `@vue/test-utils` не установлены, скрипта `test` нет.
- **Форматтера как ГЕЙТА нет.** `prettier` не установлен и в скриптах не вызывается.
  Но сам `.prettierrc` — **не призрак**: его настройки (`printWidth: 100`, `semi: false`,
  `singleQuote: true`, `tabWidth: 2`, `trailingComma: es5`) ТОЧНО совпадают с
  фактическим стилем `src/react`. Это действующая конвенция кода — соблюдай её
  руками. Не ставь `.prettierrc` в один ряд с двумя конфигами ниже.
- **CI нет.** Ни `.github/workflows`, ни другого автоматического прогона.

Единственные автоматические гейты — типы и сборка. Всё остальное ловит прораб
глазами и прогон под двумя аккаунтами. Это **записанный долг**
(`docs/project/backlog.md`), а не норма — не выдавай его за достаточную проверку.

## Окружение

```bash
cp .env.example .env
```

| Переменная | Назначение |
|---|---|
| `VITE_SUPABASE_URL` | адрес проекта Supabase |
| `VITE_SUPABASE_ANON_KEY` | публикуемый (anon) ключ |

Значения берутся в дашборде Supabase → Project Settings → API. `.env` в git не
попадает и в чат не пересказывается. **Всё с префиксом `VITE_` попадает в бандл** —
класть туда что-то секретное нельзя.

На Vercel переменные задаются отдельно, в настройках проекта: правка `.env.example`
на прод не влияет никак.

## Мост (Supabase MCP / Vercel MCP)

Инструменты, которыми агент смотрит в прод: схема, миграции, RLS, advisors,
SQL-выборки — Supabase; деплои, build- и runtime-логи — Vercel.

**Состояние: к этому проекту мост НЕ подключён.** Коннектор Supabase видит только
чужую организацию, коннектор Vercel — только чужие проекты. Проверяется двумя
вызовами `list_projects` в начале сессии. Подключение — действие прораба.

## Внешние инструменты

`scripts/fetch-equipment-images.mjs` (команда `npm run images:equipment`) требует
системных утилит macOS — `sips` (ресайз) и `cwebp` (кодирование в WebP).
Их нет — скрипт не отработает; это не ошибка проекта.

## Онбординг новой библиотеки

Новая библиотека входит в проект → получи её актуальную документацию (Context7 или
официальный источник), зафиксируй ТОЧНУЮ версию в `package.json`, создай шпаргалку
`docs/system/libs/<имя>.md` — рабочий API именно этой версии и наши конвенции — и
дальше опирайся на неё. Код по памяти не пишем.
