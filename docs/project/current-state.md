# Current State — ARGO Warehouse

**Сессия:** 15 (2026-08-23) — закрыта.
**Актуальный handoff:** `docs/handoffs/2026-08-23-15-wave-3-rls-verify.md`
**Прод:** `main` = с15 (`f8e37a2` + доки), бандл `index-WydjQBfu.js`.
**Волны UX-аудита с7 закрыты ВСЕ** (47/47). Первый `/rls-verify` выполнен: 13
операций под двумя аккаунтами, расхождений с матрицей нет.
**Приёмка глазами должна прорабу:** блоки 4–5 чеклиста с10–с13 (16 пунктов +
уборка) и `acceptance-s15-wave3.md` (U34-L, U29, тексты отказов).
**Мост:** Supabase `warehousev4` (`umbqiktyqlfqqgupxqdm`), Vercel `warehousev4`
(`prj_2Xh9KlIRla8fZaF4n8ZcSWDQ14MY`, команда `team_VVVqjT57YkCiqVBzOXs0OBfs`).
Мост может оказаться **read-only** (коннектор переподключили с галочкой):
признак — `current_user` = `supabase_read_only_user`, лечение — переподключить.

Сводка «как есть сейчас». Хроника — `docs/handoffs/` (тема в имени файла),
«как устроено» — код; библия `01-product` / `02-decisions` / `03-data-model` держит
только то, что из кода не выводится; «почему» — handoff той сессии.

## Курс

**ARGO Warehouse** — внутренняя система учёта AV-оборудования ARGO Media: сотрудник
собирает список оборудования на мероприятие, сохраняет и выгружает Excel.
**Границы продукта (решение прораба, с10):** жизненного цикла списков
(подтверждение → выдача → возврат) в продукте НЕТ и не планируется. Продукт — это
собрать список, сохранить, скачать документ.

- **Репозиторий:** `github.com/strix-kim/warehousev4`, ветка `main` = прод. Публичный.
- **Код:** `src/react` — 50 файлов, 9 135 строк. **За порогом ~800:**
  `styles.css` 1008, `ListEditorPage.tsx` 813; следом `database.types.ts` 620
  (генерируется + правки руками, не пилим), `EquipmentPage.tsx` 588 (вырос в с15
  на модельном каталоге), `ListsPage.tsx` 574. Распил — чистым переносом,
  отдельным коммитом.
- **Стек:** React 19 + TypeScript 5.9 + Vite 7 + Supabase, хостинг Vercel; Node 22.x
  (локально 24 — прод-сборку доказывает Vercel). Граница с базой типизирована
  (`createClient<Database>`; в `database.types.ts` — ПРАВКИ РУКАМИ: nullable-аргументы
  RPC и три функции с15, генератор их не выражает/не запускался).
- **Бандл (с15):** `react-vendor` 231.69 КБ / 74.11 gzip, `supabase` 113.06 / 31.30,
  собственный `index` 22.70 / 8.40, CSS 72.34 / 13.89; `ListEditorPage` 39.03 / 13.12,
  `EquipmentPage` 33.98 / 10.79 (модельный каталог + дровер модели),
  `ListsPage` 19.88 / 7.00, `EquipmentCreatePage` 19.31 / 6.21 (партия).
- **Локально:** `npm run dev` → `http://localhost:5180/` (strictPort); сборка без
  `.env` валится гейтом.
- **Данные прода (сверено выпиской в с15):** `equipment` 1481 (1 877 штук,
  218 моделей, 7 категорий / 56 подкатегорий, локация одна «Офис»),
  `equipment_movements` 1391 (след импорта), `events` 4, `mount_points` 9;
  **`users` 2 — второй аккаунт `rls-test@argomedia.uz` / `technik2026`
  (`technician`) живёт для прогонов прав**; списков 6, из них 5 тестовых
  `ТЕСТ-с10-*` (нужны блокам 4–5, удалять пунктом 46).
  Схема на 2026-08-23: **7 таблиц, 25 политик, 9 триггеров, 16 функций,
  32 индекса** (baseline + 15 миграций; baseline описывает СТАРТОВУЮ точку —
  сверять по живой базе). Четыре миграции с15: `append_equipment_to_list`
  (+ версия с jsonb-возвратом), `create_equipment_batch`, `fetch_equipment_models`.

## Каноны

- **База авторитетна.** Правило только в React — не существует.
- **`main` — прод; сливаем принятое** с зелёными `npm run check` и `npm run build`.
- **`git branch --show-current` ПЕРЕД `git add`** (с11) и **`git status` на двойники
  iCloud перед КАЖДЫМ `git add`** (с15: трижды за сессию).
- **Схема — только миграциями.**
- **Код в прод раньше схемы.** В том числе при смене ФОРМАТА ответа RPC: клиент,
  понимающий оба формата, едет первым (с15). Смена типа возврата функции —
  `drop` + `create`, гранты заново.
- **Код ошибки для клиента — из класса `PT`, не 40** (с14): `PT409` = 409 Conflict;
  `40001` для PostgREST — команда «повтори транзакцию» и бесконечный цикл.
- **`.contains()` в postgrest-js смотрит на тип ЗНАЧЕНИЯ** (с14): jsonb передают
  строкой `JSON.stringify`, иначе `{[object Object]}` и 400.
- **Гонку уникальности без UNIQUE-индекса закрывает `pg_advisory_xact_lock`
  по нормализованному значению, локи берутся отсортированно** (с15, партия
  серийников) — иначе два конкурента с пересечением ловят deadlock.
- **`CASCADE` на политике RLS — не выход** (с10): зависимую политику пересоздают явно.
- **Необратимое объявляется и исполняется тем же ходом:** запрос, число строк,
  откат текстом. Каждая `create/replace function` — с `revoke from public, anon`.
- **`revoke all on public.users` держится на `security definer`** — «починка»
  уронит вход всем (`03-data-model`).
- **Роли `admin` в проде НЕТ** (с15): оба аккаунта `technician`; удаление списков
  держится на почте `argo@argomedia.uz`, удаление единиц оборудования недоступно
  через клиента никому — только мостом.
- **`reservation_start` / `reservation_end` — это ДАТА МЕРОПРИЯТИЯ, не бронь.**
- **Состояние каталога — в адресе** (с11): `q`, `type`, `subtype`, `status`, `page`,
  `item`, с с15 ещё `mbrand`/`mmodel` (открытая модель; два параметра, не склейка).
  Прогретый первый кадр — только для нетронутого каталога.
- **Каталог модельный** (с15): строка выдачи — модель, единицы — дровером модели,
  на экране в каждый момент один дровер (модели ИЛИ единицы), «назад» шагает
  карточка → модель → каталог. Поиск по серийнику находит МОДЕЛЬ.
- **Ячейку-константу на телефоне убирают ИЗ DOM, а не прячут `display: none`** (с11).
- **Значения в `.or(...)` PostgREST квотируются** (`quoteFilterValue`) ПОСЛЕ
  `escapeLikePattern`.
- **Тексты интерфейса — только `tr(ru, uz)`.**
- **Права — прогон под двумя аккаунтами** (`/rls-verify`); теперь ВЫПОЛНИМ:
  второй аккаунт живёт в проде, матрица и процедура — handoff с15.
- **Порог входа в библию (с12):** только то, что нельзя восстановить чтением
  `src/react`.
- **`security definer` в `private.*` цел** (с12): не менять владельцев, не включать
  `force row level security`.
- **Протокол с9 сокращён** (`workflow.md`): мост на старте не проверяем,
  `decisions.md` заморожен, handoff без списков файлов.
- **Тиры с9:** сессия — Opus, Fable — только `planner` по вызову, Sonnet — `scout`.

## Долги

- **Приёмка глазами:** (а) блоки 4–5 чеклиста с10–с13 — широкий экран (30–32) и
  телефон 390 (33–45, только с телефона) + уборка тестовых списков (46);
  (б) `acceptance-s15-wave3.md` — U34-L, U29 и тексты отказов rls-сценариев.
  Код принят прорабом авансом («потом проверю») — его решение, с15.
- **Регистрация в Auth открыта всем** (`disable_signup: false`) — мусорные
  пользователи в `auth.users` доступны любому со знанием публикуемого ключа
  (членство это не даёт). Закрыть галочкой после заведения сотрудников (backlog).
- **Дыры «клиенту не верим» (остаток):** brand/model позиций в document-RPC
  по-прежнему из клиентского JSON (append-путь с15 уже берёт из базы); UNIQUE
  серийника нет — пакетный путь закрыт advisory-локом, одиночное заведение
  по-прежнему держит клиентский ilike-чек; количество не сверяется с остатком.
- **Схема vs репозиторий (с2):** `users.id` без FK на `auth.users`; удаление
  списков по почте вместо роли (теперь это единственный «админский» рычаг —
  роли admin нет ни у кого); дубль FK + четыре триггера на `mount_points`;
  неиспользуемый GIN-индекс. `baseline.sql` ни разу не исполнялся на чистой базе.
- **Auth не для внешнего запуска:** пароль А из шести нулей, пароль Б назван в
  доках открытым текстом, защита от скомпрометированных паролей выключена.
- **Доступность:** фокус-ловушек в drawer'ах нет (их теперь ТРИ вида: единица,
  модель, списки); `AppSelect` не выполняет контракт listbox; `aria-live` местами.
- **`styles.css` 1008 и `ListEditorPage.tsx` 813 — за порогом**; `EquipmentPage.tsx`
  588 и растёт. Кандидаты распила — строками в `backlog.md`.
- **Сокращённую библию прораб не читал** (с12).
- Строка «Черновик · сохранён …» после перезагрузки показывает время создания.
- Экспорта в дровере деталей нет (R-F9).
- Глубокая ссылка `?item=<id>` теперь ходит через `fetchEquipmentByIds` всегда
  (модельная выдача) — живьём после с15 не проверялась; `?mbrand/?mmodel` на
  модель вне загруженной страницы — тоже.
- `features/` в корне — untracked каталог прораба (xlsx/docx), не код;
  `.gitignore` или унести.
- Мёртвые метки `issued`/`returned` в журнале движений — строка в `backlog.md`.

## Вехи

Веха = проверяемый руками чекпоинт, не дата.

**Закрыты:** веха 1 (схема в репозитории, с2), веха 2 (приложение не падает
молча, с6). **Волны 1–3 UX-аудита с7 закрыты целиком (47/47, с8–с15).**

| Веха | Чекпоинт | Состояние |
|---|---|---|
| **3. Дыры «клиенту не верим»** | прогон под двумя аккаунтами показывает, что подделанный запрос отклоняет БАЗА | **разблокирована с15** (аккаунт есть, первый прогон пройден); осталось закрыть сами дыры: brand/model, UNIQUE серийника, количество |
| **4. Готовность к внешнему запуску** | `get_advisors` security без предупреждений выше INFO | ждёт: пароли, открытая регистрация, Postgres не обновлён |
| **5. Автоматические гейты** | PR со сломанным сохранением не проходит без прораба | ждёт: линтера, тестов и CI нет |
| **6. Доступность и полировка** | редактор проходится с клавиатуры до документа | ждёт |

## Очередь целей (топ-3)

1. **Блок новых фич прораба** — состав расскажет на старте следующей сессии
   («огромный блок», с15). Перед кодом — план и, при надобности, `planner`.
2. **Приёмка глазами:** блоки 4–5 (телефон — обязателен) + пункт 46 +
   `acceptance-s15-wave3.md`. Прораб делает сам, агент готовит разбор.
3. **Веха 3, сами дыры:** триггер brand/model из `equipment`, UNIQUE серийника
   (после физической сверки наклеек), сверка количества с остатком.
