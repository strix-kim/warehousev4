# Current State — ARGO Warehouse

**Сессия:** 11 (2026-08-22) — закрыта.
**Актуальный handoff:** `docs/handoffs/2026-08-22-11-ux-wave-2-registry-catalog.md`
**Прод:** `main` = с11 целиком (черновик карточкой в реестре, мобильная карточка
каталога, состояние каталога в адресе и «показать ещё»), деплой
`dpl_Hukxk3HnuryJRM37iKLvngqbMBDh` READY на `abc9a64`.
**Мост:** Supabase `warehousev4` (`umbqiktyqlfqqgupxqdm`), Vercel `warehousev4`
(`prj_2Xh9KlIRla8fZaF4n8ZcSWDQ14MY`, команда `team_VVVqjT57YkCiqVBzOXs0OBfs`).

Сводка «как есть сейчас». Хроника — `docs/handoffs/` (тема в имени файла),
«как устроено» — код; библия `01-product` / `02-decisions` / `03-data-model` держит
только то, что из кода не выводится; «почему» — handoff той сессии.

## Курс

**ARGO Warehouse** — внутренняя система учёта AV-оборудования ARGO Media: сотрудник
собирает список оборудования на мероприятие, сохраняет и выгружает Excel.
**Границы продукта (решение прораба, с10):** жизненного цикла списков
(подтверждение → выдача → возврат) в продукте НЕТ и не планируется. Продукт — это
собрать список, сохранить, скачать документ.

- **Репозиторий:** `github.com/strix-kim/warehousev4`, ветка `main` = прод. Публичный.
- **Код:** `src/react` — 45 файлов, 7 978 строк. **За порогом ~800:** `styles.css` 900,
  `ListEditorPage.tsx` 811 (перешагнул в с11); следом `database.types.ts` 579
  (генерируется, не пилим), `ListsPage.tsx` 574, `EquipmentPage.tsx` 510.
  Распил — чистым переносом, отдельным коммитом.
- **Стек:** React 19 + TypeScript 5.9 + Vite 7 + Supabase, хостинг Vercel; Node 22.x
  (локально 24 — прод-сборку доказывает Vercel). Граница с базой типизирована
  (`createClient<Database>`, `lib/database.types.ts` снят с прода 2026-08-22;
  в нём четыре ПРАВКИ РУКАМИ — nullable-аргументы RPC, генератор их не выражает).
- **Бандл (с11):** `react-vendor` 231.69 КБ, `supabase` 113.06, собственный `index`
  20.52 / 7.82 gzip, CSS 68.15 / 13.29; `ListEditorPage` 36.69 / 12.45,
  `EquipmentPage` 23.71 / 8.06, `ListsPage` 19.89 / 7.00.
- **Локально:** `npm run dev` → `http://localhost:5180/` (strictPort); сборка без
  `.env` валится гейтом.
- **Данные прода (2026-08-22, схема и данные в с11 НЕ менялись):** `equipment` 1481
  (1 877 штук, 218 моделей, 7 категорий / 56 подкатегорий, локация одна «Офис»),
  `equipment_movements` 1391 (все `status_normalized` — след импорта, к выдаче
  отношения не имеет), `events` 4, `mount_points` 9, `users` 1 — **аккаунт ровно
  один**; списков 6, из них **5 тестовых `ТЕСТ-с10-*`** (см. «Долги»).
  Схема: baseline (134 объекта) + 10 миграций.

## Каноны

- **База авторитетна.** Правило только в React — не существует.
- **`main` — прод; сливаем принятое** с зелёными `npm run check` и `npm run build`.
- **`git branch --show-current` ПЕРЕД `git add`, а не после** (с11). Коммит, сделанный
  стоя на `main`, уезжает в прод мимо рабочей ветки, а ветка молча отстаёт — и
  переключение в неё откатывает рабочее дерево вместе с недописанной правкой.
- **Схема — только миграциями.**
- **Код в прод раньше схемы.** Удаляешь колонку или таблицу — сначала выкати код,
  который её не просит, дождись READY и сверь хэши бандла, и только потом миграцию.
  Обратный порядок роняет живой прод (с10).
- **`CASCADE` на политике RLS — не выход.** Таблица без INSERT-политики под RLS
  запрещает вставку ВСЕМ; зависимую политику пересоздают явно (с10).
- **Необратимое объявляется и исполняется тем же ходом:** `DELETE`/`UPDATE` по
  живым данным — запрос, число строк, откат текстом, без паузы. Каждая
  `create/replace function` — с `revoke from public, anon` (gotchas §3).
- **`revoke all on public.users` держится на `security definer`** — «починка»
  уронит вход всем (`03-data-model`).
- **`reservation_start` / `reservation_end` — это ДАТА МЕРОПРИЯТИЯ, не бронь.**
  Имя осталось от удалённой подсистемы; на этих колонках стоят карточка реестра и
  фильтр периода. Не чистить «за компанию».
- **Состояние каталога — в адресе** (с11): `q`, `type`, `subtype`, `status`, `page`,
  `item`. Локальным остался только `searchInput` (дебаунс 350 мс). Прогретый первый
  кадр читается ТОЛЬКО для нетронутого каталога — под фильтром кэш без фильтра
  отвечал бы не на заданный вопрос.
- **Ячейку-константу на телефоне убирают ИЗ DOM, а не прячут `display: none`** (с11):
  спрятанный элемент остаётся в `:last-child` и `nth-child` и ломает чётность грида.
- **Значения в `.or(...)` PostgREST квотируются** (`quoteFilterValue`), и только
  ПОСЛЕ `escapeLikePattern`. Порядок слоёв некоммутативен.
- **Тексты интерфейса — только `tr(ru, uz)`.**
- **Права — прогон под двумя аккаунтами** (`/rls-verify`); пока **невыполним**:
  аккаунт один.
- **Протокол с9 сокращён** (`workflow.md`, шапка): мост и окружение на старте не
  проверяем, `decisions.md` заморожен, handoff без списков файлов, библия без
  номеров строк, current-state без журнала.
- **Тиры с9:** сессия — Opus (`"model": "opus"` в проектном `settings.json`),
  Fable — только `planner` по вызову, Sonnet — `scout`. Раздел «Тиры моделей»
  `CLAUDE.md` — источник правды; называть раскладку перед воркфлоу.

## Долги

- **ЧЕТЫРЕ слоя UI выкачены в прод НЕПРОВЕРЕННЫМИ глазами** — весь с10 плюс три
  чекпоинта с11, все приняты авансом. **Это потолок: поверх строить нельзя.**
  Чеклисты — в хэндоффах с10 и с11; начинать с телефона 390 px, там видны две
  правки из трёх.
- **5 тестовых списков в проде** (`ТЕСТ-с10-*`) — оставлены, пока приёмка не
  прогнана: без них поиск по заказчику и фильтр периода проверять не на чем.
  Удалить после: `delete from equipment_lists where name like 'ТЕСТ-с10%' or name
  like 'ТЕСТ_с10%'`. **Числа чеклиста с10 сместились:** «следующий месяц» = 2, не 1.
- **Второй аккаунт в проде** — блокирует `/rls-verify` пересозданной RPC
  `update_equipment_model_and_unit` (с5), пересозданной INSERT-политики
  `equipment_lists` (с10), волну 3 UX и ветки с8, видимые только второму
  аккаунту (отказ прав на удалении, пустой каталог без строки в `users`).
- **Дыры «клиенту не верим» (серверная часть):** brand/model позиций не сверяются с
  оборудованием; UNIQUE серийника нет.
- **Схема vs репозиторий (с2):** `users.id` без FK на `auth.users`; удаление списков
  по почте вместо роли; дубль FK + четыре триггера на `mount_points`; неиспользуемый
  GIN-индекс. `baseline.sql` ни разу не исполнялся на чистой базе.
- **Auth не для внешнего запуска:** пароль из шести нулей, защита от
  скомпрометированных паролей выключена (ждёт обновления Postgres).
- **Доступность:** фокус-ловушек в drawer'ах нет; `AppSelect` не выполняет контракт
  listbox; `aria-live` только на логине и в строке состояния редактора.
- **`styles.css` 900 строк и `ListEditorPage.tsx` 811 — оба за порогом.** Кандидат в
  редакторе намечен строкой в `backlog.md`: кластер восстановления черновика.
- **Чеклисты приёмки с5/с6 по проду прораб не прогонял** (ссылки — в хэндоффах).
- `features/` в корне — чужой untracked каталог прораба; `.gitignore` или унести.
- Строка «Черновик · сохранён …» после перезагрузки показывает время создания
  (`updated_at` не в выборке списка).
- Экспорта в дровере деталей нет (R-F9): скачивание только с карточки.
- Глубокая ссылка `?item=<id>` на позицию вне текущей выборки тянет строку через
  `fetchEquipmentByIds` — путь написан, живьём не проверялся (с11).

## Вехи

Веха = проверяемый руками чекпоинт, не дата. Порядок — предложение агента; прораб
может перебить, тогда веха сдвигается вниз, а не исчезает.

**Закрыты:** веха 1 (схема в репозитории, с2) и веха 2 (приложение перестаёт падать
молча — границы ошибок, самолечение чанка, канал диагностики, бейдж возраста, с6).

| Веха | Чекпоинт | Состояние |
|---|---|---|
| **3. Дыры «клиенту не верим»** | прогон под двумя аккаунтами показывает, что подделанный запрос отклоняет БАЗА, а не интерфейс | **заблокирована:** аккаунт один |
| **4. Готовность к внешнему запуску** | `get_advisors` по безопасности не даёт предупреждений выше INFO | ждёт: пароль из шести нулей, защита от скомпрометированных паролей выключена, Postgres не обновлён |
| **5. Автоматические гейты** | PR со сломанным сохранением не проходит проверку без участия прораба | ждёт: линтера, тестов и CI в проекте нет |
| **6. Доступность и полировка** | проход по редактору только с клавиатуры доводит до сохранённого документа | ждёт: фокус-ловушки, контракт `AppSelect`, `aria-live` |

## Очередь целей (топ-3)

1. **Приёмка четырёх слоёв по чеклистам с10 и с11 + правка найденного**, затем
   удалить тестовые списки. Дальше строить поверх непроверенного нельзя.
2. **Второй аккаунт в проде + первый настоящий `/rls-verify`** — разблокирует
   волну 3 (U2, U29, U30-M, U34-L, U35-б) и веху 3 «клиенту не верим».
3. **Остатки волны 2:** U45, U35-а, U37-M, U44 (без ТВ), U3-M автосейв.

Из волны 2 сделано: U1 и U32 (с8), U6 и U15 (с9), U20 и U21 (с10), **U24, U33, U36,
U39 (с11)**. `/doc-audit` из очереди снят — библия сокращена в с12 и пересверена по
живой схеме. Вне очереди: распил `styles.css` (942 со свежей шапкой) и
`ListEditorPage.tsx` (811) чистым переносом.
