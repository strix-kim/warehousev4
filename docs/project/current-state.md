# Current State — ARGO Warehouse

**Сессия:** 23 (2026-08-24) — закрыта.
**Актуальный handoff:** `docs/handoffs/2026-08-24-23-perf-ui-audit.md`
**Прод:** `main` = `9ce1f79`, деплой `abbe28b` (залы целиком). **С с23 в прод НИЧЕГО
не поехало.** Ветка `session-23-perf-ui` (`6b771ea`) **не влита и глазами не принята**
— следующая сессия стартует ОТ НЕЁ, а не от `main`, иначе прочитает прошлый
current-state.
**Залы:** редактор, ТВ, печать и текст для чата на проде. НЕТ: **PDF-выгрузки файлом**
(цель отложена прорабом в с23, из очереди не выпала).
**Папка проекта — `warehouse.argomedia.uz.nosync`** (iCloud отключён), на старом
пути симлинк — работать по прежнему пути.
**Приёмка глазами должна прорабу:** `6b771ea` (две минуты, см. ниже), шесть слоёв с19,
интерактив ТВ на реальном экране планёрки, печать в Safari.
**Мост:** Supabase `warehousev4` (`umbqiktyqlfqqgupxqdm`), Vercel `warehousev4`
(`prj_2Xh9KlIRla8fZaF4n8ZcSWDQ14MY`, команда `team_VVVqjT57YkCiqVBzOXs0OBfs`).
**Мост read-only с с22** (`current_user = supabase_read_only_user`): `execute_sql`
только читает, `set role authenticated` запрещён. Писать в прод — через живой REST
с JWT (gotchas §2) либо скриптом прорабу. В с23 мост не звали ни разу.
Версии миграций в проде штампуются `apply_migration` (имена файлов ≠ версии в
`schema_migrations` — паттерн, не поломка).

Сводка «как есть сейчас». Хроника — `docs/handoffs/`, «как устроено» — код;
библия `01-product` / `02-decisions` / `03-data-model` — только невыводимое из
кода; «почему» — handoff той сессии.

## Курс

**ARGO Warehouse** — внутренняя система ARGO Media. Ядро: списки оборудования на
мероприятие + Excel. Расширение: сотрудники (с17), автомобили (с18), экспорт
«на мероприятие» (с19), распределение по залам (с20–с22). **Текущая фаза (с23):
три новых модуля работают, но не отполированы — впереди вид, потом скорость.**

- **Репозиторий:** `github.com/strix-kim/warehousev4`, `main` = прод. Публичный.
- **Код:** `src/react` — 97 файлов, 17 864 строки. **За порогом ~800:**
  `styles.css` 1132, `database.types.ts` 1057 (генерируется + правки руками, не
  пилим), `ListEditorPage.tsx` 813.
- **Стек:** React 19 + TypeScript 5.9 + Vite 7 + Supabase + Vercel; Node 22.x.
  Пять рантайм-зависимостей, ни одного файла шрифта, ни UI-кита, ни state-менеджера,
  ни react-query. Свой OOXML (`lib/xlsx/`). `database.types.ts` правится руками.
- **Локально:** `npm run dev` → `http://localhost:5180/` (strictPort). После
  переноса/переименования каталога dev-сервер перезапускать обязательно.
- **Данные прода** (снято в с22, в с23 не перечитывали): `equipment` 1482 (218
  моделей), `employees` 12, `vehicles` 6, **`vehicle_files` ПУСТА** (весь фото-путь
  машин в проде ни разу не отрабатывал), `equipment_lists` 5 (4 мусорных `ТЕСТ-с10-*`,
  см. долги); залы: 1 план (6 залов, 5 позиций, 8 ячеек), `position_catalog` 6.
  `users` 2 — оба `technician`; второй — `rls-test@argomedia.uz` / `technik2026`.
  Схема: **17 таблиц, 62 политики public + 4 storage, 21 триггер, 61 индекс**
  (baseline + 28 миграций).

## Каноны

- **База авторитетна.** Правило только в React — не существует.
- **`main` — прод; сливаем принятое** с зелёными `npm run check` и `npm run build`.
- **Тиры (с18): ведущего выбирает прораб**; код — `coder` (Opus), планы —
  `planner` (Fable), механика — `scout` (Sonnet). Раскладку называть перед
  воркфлоу.
- **Аудит отвечает на тот вопрос, который ему задали (с23).** «Что нарушено» и
  «как сделать лучше» — разные жанры, и второй не содержится в первом. Формулировку
  прораба «сделать UI продакшн-уровня» читать вслух ДО запуска: список дефектов или
  предложение облика?
- **Перф-находку без объёма данных не принимать (с23).** Скептик со свежим чтением
  прода снял 18 находок из 20: «полноразмерные фото» — 12 файлов по 77 КБ,
  «последовательная загрузка фото машин» — при пустой таблице.
- **Возражение агента против плана ведущего — не шум (с23).**
- **Кэш чтений — `persistentCache`, и он ЕСТЬ у equipment/lists и НЕТ у трёх новых
  модулей** (с23). Отсюда мигание на «сохранил → назад». Класс задач и порядок —
  backlog; **`fetchEmployees` кэшировать только `{ persist: false }`** (`select('*')`
  тянет паспорт, ПИНФЛ, адрес прописки — им не место в `localStorage`).
- **Кэш плана залов опасен:** список планов кэшировать можно, сам план и ТВ — нет
  (общий черновик, ТВ поллит каждые 30 с).
- **Прогон прав без второго браузера (с22):** мост read-only, `/rls-verify` гоняется
  по живому REST прода настоящим JWT второго аккаунта. Рецепт — gotchas §2.
- **`HTTP 200` + ноль строк = отказ политики,** а не успех (с22).
- **Залы — матрица (с20):** позиции — строки ВСЕГО мероприятия, залы — колонки,
  ячейка = максимум один человек ИЛИ слот «Наём» (UNIQUE `cell_key`); пустая
  ячейка и есть вакансия, слот — «решено: берём внешнего» (с21).
- **Слот «Наём» (с21):** `is_external` + CHECK «(employee_id is null) =
  is_external»; кнопка — только в операторских строках (правило в UI, backlog);
  FK на employees — `on delete cascade`.
- **Имена в расстановке — «Имя Фамилия»** (`employeeDisplayName`, с21).
- **Залы — общий черновик staff (с20/с21, подтверждено прогоном с22):** изоляции
  между staff нет по замыслу. **Один менеджер в работе (с23, прораб)** — гонок двух
  вкладок в расстановке можно не бояться.
- **ТВ `/halls/:planId/tv` (с21) — инструмент планёрки:** назначение кликом,
  структура только в редакторе; поллинг 30 с через `silentRefresh`; портал
  пикера — `fullscreenElement ?? body`.
- **Печать матрицы (с22):** цвет зала — ЧЕРТА под именем; панель и шапка —
  `display: contents`; серые токены темнеют внутри `@media print`; поле сверху 14 мм.
  Проверять только по самому PDF, скриншот врёт. gotchas §14. **ВНИМАНИЕ (с23):
  блок написан глобальными селекторами и течёт на другие страницы — см. долги.**
- **Текст для чата (с22):** по залам, не матрицей; **без markdown** — Telegram не
  разбирает разметку во вставленном тексте.
- **`document.title` — владелец один** (`LanguageProvider`), страница называет
  себя через `useDocumentTitle` (с22). **Стоит ровно в одном месте** — три вкладки
  подписаны одинаково (с23, долг).
- **Директор не назначается на залы** (с21).
- **Safari: кнопка с `autoFocus` + `onBlur`-гашением обязана иметь
  `onMouseDown preventDefault`** (с21, gotchas §6).
- **Справочник позиций (с20):** общий на всех; FK из планов НЕТ намеренно (снимок).
- **Один человек в нескольких ячейках — норма**; скрепка = несколько позиций
  в ОДНОМ зале, ×N = по всему плану.
- **Роль позиции: иконка без подписи** (с21, подтверждено для печати в с22).
- **`git branch --show-current` перед `git add`** — привычка остаётся.
- **Схема — только миграциями.**
- **Фото в xlsx: якорь `oneCellAnchor` + АБСОЛЮТНЫЕ координаты в `a:xfrm`** (с19/с20).
- **Фото для документов — `employees.document_photo_id`** (с19), NULL →
  последнее загруженное.
- **Язык документа ≠ язык интерфейса** (с19); **новые стили xlsx — только в хвост
  `styles.ts`** (с19); **руководитель для документов — константа `companyDirector`** (с19).
- **Госномер: канон в базе** (с18); **машина↔водитель — M2M** (с18);
  **сотрудники: уникальность по документам** (с17); **файлы — только добавление** (с17/с18).
- **Signed URL — только in-memory кэш** (с17); **фото ужимает клиент** (с17).
- **UX проектирует агент** (с17) — **включая визуальную концепцию (с23)**;
  **образец «как делаете сейчас» выпрашивать ДО проектирования** (с20).
- **Пачечный upsert/insert под RLS — `defaultToNull: false`** (с18).
- **Ошибки вставки разбирать по ИМЕНИ ограничения** (с14), код клиенту — из
  класса `PT` (с14); `.contains()` — тип значения (с14).
- **Состояние каталога — в адресе** (с11/с15); каталог модельный (с15);
  серийник опционален (с16).
- **Тексты интерфейса — только `tr(ru, uz)`.**
- **Права — `/rls-verify`;** RPC и Storage — smoke curl-ом.
- **Порог входа в библию (с12):** только невосстановимое чтением `src/react`.
- **`security definer` в `private.*` цел** (с12); `revoke from public, anon` на
  каждой новой функции. **Роли `admin` в проде НЕТ** (с15).
- **`reservation_start`/`reservation_end` — дата мероприятия, не бронь.**

## Долги

- **`6b771ea` не принят глазами и не влит.** Проверка на две минуты: завести или
  переименовать сотрудника → вернуться в план залов → он в пикере с новым именем.
  Соврёт — инвалидация сломана, откат.
- **Визуальная концепция трёх модулей не сделана** — аудит с23 был дефектным, а не
  проектным. Цель №1.
- **Четыре поломки, найденные аудитом с23** (не «медленно» и не «некрасиво», а брак):
  `print-leak` — печать каталога портится после захода в «Залы» (`@media print` в
  `halls.css` глобальными селекторами, `<style>` чанка не удаляется);
  `hall-editor-header-mobile` — на телефоне спрятан статус «Не сохранилось»;
  `mono-input-13px-ios-zoom` — iOS зумит четыре поля и не возвращает;
  `hall-palette-contrast` — три цвета зала из восьми дают 3,30 / 3,55 / 3,71 при
  норме 4,5, на ТВ имя зала не читается.
- **Сквозной проход по ощущаемой скорости** — backlog, порядок задан прорабом: ПОСЛЕ
  вида. Три круга сети в списке сотрудников, отсутствие `persistentCache` у новых
  модулей, `DataAge` на двух списках из шести, `AppShell` качает 1.4 МБ каталога
  через 700 мс после входа в ЛЮБОЙ раздел.
- **Печать рвётся на второй лист без повтора шапки залов** (>~17 строк на десяти
  залах) — лечится переводом печати на `<table>` (backlog).
- **Ловушки фокуса в дроверах нет** (`lib/useModalLayer.ts` вешает только Escape и
  блокировку прокрутки при объявленном `aria-modal`) — все девять дроверов системы.
  `AppSelect` не listbox; матрица и ТВ с клавиатуры не проходятся.
- **Печать в Safari не проверена**; интерактив ТВ на большом экране — тоже.
- **Права: не-staff роль (`video_engineer`) и DELETE чужой записи** живым запросом
  не испытаны (backlog). Остальное по залам прогнано в с22.
- **Удаление списков зашито на почту** в политике `equipment_lists`; четыре
  `ТЕСТ-с10-*` в проде не убрались — сносить прорабу из интерфейса (backlog).
- Узбекские тексты (документы с19, залы с20–с22) — носителем не проверены; **длина
  узбекских строк не читалась вовсе** (с23) — ломает тулбары и кнопки.
- Фото на телефоне после фикса `a:xfrm` — перевыгрузка не подтверждена.
- `catalogState` в `useHallPlanEditor` пишется, но не читается.
- Удаление фото сотрудника — решение прораба не получено (с19, backlog).
- Дыры «клиенту не верим» (оборудование): brand/model в document-RPC, UNIQUE
  серийника, количество. Слот «Наём»: роль строки не проверяется базой (backlog).
- Сирота в бакете при упавшей второй ступени (с17); отказ RLS на update
  неотличим от ошибки полей; не-staff видит кнопки записи; signed URL живёт час
  у любого держателя (принято, с17).
- Схема vs репозиторий (с2): `users.id` без FK на `auth.users`; дубль FK на
  `mount_points`; `baseline.sql` не исполнялся на чистой базе.
- `styles.css` 1132 и `ListEditorPage.tsx` 813 — за порогом; мёртвый
  `.quantity-stepper` в styles.css (backlog).
- Плитки «Автомобили» и «Залы» на главной без иллюстраций — арт за прорабом.
- Экспорта в дровере деталей нет (R-F9); мёртвые метки `issued`/`returned`;
  `features/` в корне — untracked каталог прораба.

## Вехи

**Закрыты:** 1 (схема в репо), 2 (не падает молча), волны UX-аудита 1–3.

| Веха | Чекпоинт | Состояние |
|---|---|---|
| **3. Дыры «клиенту не верим»** | подделанный запрос отклоняет БАЗА | остаток — оборудование |
| **4. Внешний запуск** | advisors security чисто | ждёт |
| **5. Автоматические гейты** | CI ловит сломанное сохранение | ждёт |
| **6. Доступность** | редактор с клавиатуры | ждёт; дроверы без ловушки фокуса (с23) |
| **7. Блок фич с17** | залы на ТВ | функционально закрыт; остаток — PDF файлом |
| **8. Вид продакшн-уровня** (с23) | три модуля выглядят одним продуктом | **новая, цель №1** |

## Очередь целей (топ-3)

1. **Визуальная концепция трёх новых модулей** — проектный заход, не дефектный:
   как выглядят экраны, если рисовать заново, оставаясь в пяти зависимостях и без
   UI-кита. Карточки или таблицы, где иллюстрации, плотность, главная как витрина,
   ТВ как отдельный жанр. **До неё сессии 2–6 из плана аудита не начинать** —
   полировать экраны, которые концепция заменит, значит выбросить работу.
2. **Четыре поломки** (сессия 1 плана аудита) — от концепции не зависят, можно
   первыми или параллельно. Начинать с развязки печати: 40–60 минут, чинит
   регрессию с22.
3. **Первый кадр и кэш** (сессия 7 плана аудита) — `persistentCache` трём модулям,
   `DataAge`, `{ persist: false }` на `fetchEmployees`, гейт прогрева каталога.

Ниже очереди, не выпало: **PDF-выгрузка матрицы файлом** (отложена прорабом в с23;
план в общих чертах — jspdf ленивым импортом, кириллический TTF в репозиторий),
**дыры «клиенту не верим» оборудования** (веха 3), **хвосты экспорта** (backlog с19).

Плюс постоянный долг: приёмка глазами (слои с19, ТВ на экране, печать в Safari) —
прорабом.
