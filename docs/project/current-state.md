# Current State — ARGO Warehouse

**Сессия:** 1 (2026-08-20) — закрыта
**Актуальный handoff:** `docs/handoffs/2026-08-20-1-harness-setup.md`
**Ветка:** работа велась в `main` осознанно — правил кода не было, только доки и
конфиги. Со следующей сессии правило ветки действует — см. канон ниже
(`main` считается продом; само допущение ещё не проверено).
**Последний doc-audit:** не проводился — библия написана в с1 и по определению
сведена с кодом. Следующий рубеж — ≈с6.
**Мост:** НЕ подключён (ни Supabase, ни Vercel не видят этот проект).

Сводка «как есть сейчас», НЕ журнал. Хроника сессий — в `docs/handoffs/`,
«почему решили X» — `docs/project/decisions.md` (grep по ключу), «как устроено» —
библия `docs/project/01–06`.

## Курс

**ARGO Warehouse** — внутренняя система учёта AV-оборудования ARGO Media.
Сотрудник открывает систему, собирает список оборудования на мероприятие, сохраняет
его и выгружает Excel — и едет на выезд. Всё остальное подчинено этому сценарию.

- **Репозиторий:** `github.com/strix-kim/warehousev4`, ветка `main`.
- **Стек:** React 19 + TypeScript 5.9 + Vite 7 + Supabase (Postgres/Auth/Data API),
  хостинг Vercel. Точные версии — `docs/system/tooling.md`.
- **Рабочая копия свежая:** нет `node_modules`, нет `.env`, нет `.vercel/`,
  нет `supabase/config.toml`. Прямо сейчас проект локально не поднимется —
  нужен `npm install` и `.env` с ключами из дашборда.
- **Продукт переписан с Vue на React.** Vue-версия выведена из продукта целиком:
  события, точки монтажа, отчёты, роли, UI Kit в новый продукт НЕ входят.

## Каноны (нарушать нельзя, не переспросив)

- **База авторитетна.** Права, видимость, целостность и уникальность живут в Postgres.
  Правило, существующее только в React, не существует.
- **`main` — это прод (ДОПУЩЕНИЕ, не проверено).** В репозитории есть `vercel.json`,
  но публикует ли Vercel из `main` — в с1 не проверялось: моста нет. Пока не проверено,
  работаем осторожно: работа в ветке, push ветки свободен (превью-деплой), **слияние
  в `main` — только по явной команде прораба.** Проверить в первой рабочей сессии.
- **Схема — второй источник правды.** Живёт вне git; любое изменение фиксируется
  миграцией в `supabase/migrations/`, даже если применено кликами в дашборде.
  `scripts/*.sql` — архив без гарантий применения, новые SQL туда не кладём.
- **Мёртвая зона: всё в `src/`, что не `src/react/`** — 173 файла легаси-Vue,
  37 636 строк. Не читать, не править, не брать образцы, не «чинить».
- **Запись в прод-базу — только с разрешения прораба**, на каждое действие отдельно.
  Чтение свободно, когда мост подключён.
- **`revoke all on public.users` держится на `security definer`.** Авторизация цела
  ИМЕННО потому, что `private.is_app_member()` и `private.has_any_role()` объявлены
  `security definer`. «Починка» revoke уронит вход всем — разбор в `03-data-model`.
- **Тексты интерфейса — только через `tr(ru, uz)`.** Это не словарь, а функция:
  вызвал — обязан дать оба языка.
- **Прогон под двумя аккаунтами обязателен** для всего, что касается прав, ролей и
  чужих записей. Второй ПРОФИЛЬ браузера, не вторая вкладка: сессия живёт в
  `localStorage` и вкладки делят аккаунт.

## Работает

Механики поимённо — `02-features`, схема и права — `03-data-model`, провода —
`04-architecture`, вид — `06-design-system`. Здесь только то, о чём легко забыть.

### Продукт

- **Каталог оборудования:** поиск с дебаунсом, фильтры, постраничная загрузка,
  префетч следующей страницы, превью-drawer по клику на строку. Клик по строке
  открывает превью, добавление — отдельной кнопкой (сделано по фидбеку аудита:
  случайное нажатие меняло список).
- **Создание оборудования** работает; серийные и количественные позиции различаются.
- **Редактор списка:** два режима — «по единицам» (`specific`, серийные) и
  «по количеству» (абстрактный). Смешанный список сохраняет реальные `equipment_id`,
  а не превращается в абстрактный план.
- **Жизненный цикл списка** `draft → confirmed → issued → returned` с историей этапов
  и историей движения. **Переходы проверяются СЕРВЕРОМ** — клиентские гварды здесь
  дублируют серверные, а не подменяют их.
- **Экспорт в Excel** — самописный ZIP (метод store, ручной CRC32) и OOXML строками,
  без внешних библиотек.
- **Два языка** ru / uz, переключатель в сайдбаре, язык в `localStorage`.
- **Вход** через Supabase Auth; все шесть маршрутов за гейтом сессии.

### Инфраструктура

- **Кэш `persistentCache`** с TTL: страницы оборудования и списков — 10 минут,
  справочник категорий — 24 часа; после записи соответствующий кэш сбрасывается.
  При сетевой ошибке отдаёт старое значение — см. долги, это же и риск.
- **Ленивая загрузка всех шести маршрутов** + три отложенных прогревочных таймера
  в `AppShell` (модули, данные, данные редактора). Основной бандл ≈354 КБ
  (109 КБ gzip) по замеру аудита 19.08.
- **RLS включён** на рабочих таблицах; индексы под внешние ключи добавлены,
  `search_path` у четырёх trigger-функций зафиксирован (аудит 19.08).
- **`vercel.json`:** immutable-кэш на год для `/assets/` и `/equipment-images/`,
  неделя для `/brand/` и `/illustrations/`, security-заголовки, SPA-рерайт.
- **Фото оборудования:** 176 WebP из 217 моделей; для 41 — категорийная пиктограмма.
  Контракт именования задаёт `EquipmentVisual`.

### Харнесс (заведён в с1)

- `CLAUDE.md` + парное зеркало `AGENTS.md` (его же читает Cursor вместо снесённых
  `.cursorrules`).
- `docs/system/workflow.md` — протокол сессий. **Общий для всех проектов прораба:**
  ритуалы, шаги и шаблон хэндофа совпадают, расходятся только «Гейты стека» и «Мост».
- Шесть скиллов: `session-start`, `checkpoint`, `session-close`, `doc-audit`,
  `rls-verify`, `deploy-verify`.
- Три субагента: `planner` (Fable/xhigh), `coder` (Opus/high), `scout` (Sonnet/medium).
- `docs/system/tooling.md` (версии, команды, чего НЕТ) и `docs/system/gotchas.md`
  (справочник граблей с триггерной картой, засеян десятью разделами).
- Библия `docs/project/01–06` написана по коду с проверкой каждого якоря.

## В процессе / долги

Полный список — `backlog.md`. Здесь то, что меняет планирование.

- **Мост не подключён ни к Supabase, ни к Vercel.** Коннектор Supabase видит только
  организацию с проектом «My moda LMS», Vercel — только `my-moda-lms-*` и
  `hey-flowers-ai`. Агент не видит ни схемы, ни логов, ни деплоев; всё, что он
  говорит о проде, — реконструкция по коду. Подключение — действие прораба.
- **DDL базовых таблиц в репозитории отсутствует** (`users`, `equipment`,
  `equipment_lists`, `events`, `mount_points`, `reports`). **Прод-схема из git не
  восстанавливается**, и вся библия по колонкам — реконструкция по TypeScript-типам,
  а не выписка из базы. Это первый долг по важности и первая веха роадмапа.
- **Два механизма SQL, порядок применения неизвестен.** `supabase/migrations/`
  (4 файла) против `scripts/*.sql` (6 файлов); ни `config.toml`, ни пакета `supabase`,
  ни CI. Конкретное следствие: grant на `public.users` в одном файле против revoke
  в другом — кто победил, зависит от порядка, а порядок нигде не записан.
- **Отказоустойчивости нет.** ErrorBoundary в проекте отсутствует, все страницы —
  через `React.lazy`, `RouteBoundary` это только `Suspense` без catch. Выкатка нового
  билда при открытой вкладке → старые чанки исчезли → SPA-рерайт отдаёт HTML вместо
  JS → **белый экран всего приложения**. Плюс: ни одного `console.*` во всём
  `src/react`, сборщика ошибок нет — отказ у пользователя невидим. Плюс: офлайн не
  отслеживается, а кэш молча отдаёт старое.
- **Legacy-фолбэк списков срабатывает на ЛЮБОЙ ошибке**, без фильтра по коду (соседние
  функции гейтятся по `PGRST202/42883/PGRST205/42P01`). Сетевой сбой или 401 → все
  списки становятся «черновиками», выданные прячутся, **и это кэшируется на 10 минут.**
  Две копии кода.
- **Шесть дыр «клиенту не верим»** — перечень с якорями в `02-features`, раздел
  «Где клиент решает за базу». Самая дорогая: сервер пишет brand/model дословно из
  клиентского JSON, а дефицит считается джойном склада по этому тексту. Самая
  коварная: проверка уникальности серийного номера обёрнута в `try/catch`, где
  `catch` возвращает «дубля нет» — она отваливается при любом сбое запроса, а не
  только в гонке. Отдельно зафиксировано, что переходы статусов списка — НЕ дыра:
  клиентские гварды там дублируются серверными проверками.
- **Автоматических гейтов только два** — `tsc -b` и `vite build`. Линтера, тестов
  и CI нет; `.eslintrc.json` и `vitest.config.js` снесены в с1 как конфиги-призраки
  от Vue-стека. `.prettierrc` НЕ призрак — его настройки совпадают с фактическим
  стилем кода, это действующая конвенция без гейта.
- **Расхождение Node:** `engines.node` = 22.x, на машине 24.18.0. Локальная сборка
  не доказывает прод-сборку.
- **Безопасность прода не готова к внешнему запуску:** защита от скомпрометированных
  паролей отключена, MFA нестрогая, пароль из шести нулей, доступно обновление
  Postgres с исправлениями безопасности. Годится для закрытого теста, не для публики.
- **11 повторяющихся нормализованных серийников** — автоматически не чинятся,
  нужна физическая сверка наклеек.
- **Доступность:** фокус-ловушки в drawer'ах нет, фокус не возвращается на источник;
  `AppSelect` объявляет контракт listbox, но не выполняет его; `aria-live` нет нигде,
  кроме логина.
- **`styles.css` 889 строк** — единственный файл за порогом ~800.
- **`README.md` уличён во лжи**, `AUDIT-2026-08-19.md` построчно не проверялся и
  дублирует роли current-state и backlog — судьбу обоих решать.
- **Не прочитано полностью:** `xlsxExport.ts` (экранирование пользовательского текста
  в XML), тело RPC `create_equipment_list_document`, DML-часть большого SQL-скрипта
  (идемпотентность повторного прогона).

## Очередь целей (топ-3)

Прораб объявит задачу следующей сессии сам — очередь ниже: предложение агента.
Обоснование порядка — цена отказа, не бизнес-ценность (`decisions.md`, с1).

1. **Поднять мост и восстановить схему в репозитории** (веха 1 роадмапа).
   Пока DDL нет в git, любая работа с данными идёт вслепую, а библия по колонкам
   остаётся реконструкцией. Первым действием — подключить коннекторы; это делает
   прораб, агент без этого слеп.
2. **Приложение перестаёт падать молча** (веха 2): граница ошибок, обработка провала
   чанка после выкатки, индикатор офлайна и возраста кэша, починка legacy-фолбэка
   списков в обеих копиях.
3. **Закрыть дыры «клиенту не верим»** (веха 3), начиная с brand/model: сервер должен
   брать их из `equipment` по `equipment_id`, а не из клиентского JSON.

Ниже топ-3, не отменено: **безопасность Auth перед внешним запуском** ·
**линтер, тесты и CI** · **41 модель без фотографии** · **доступность drawer'ов** ·
**судьба README и AUDIT** · **разбиение `styles.css`** · **тест с сотрудниками на
реальном выезде**.

## Хроника вех

Указатель, а не пересказ: подробности каждой сессии — в её handoff'е
(`docs/handoffs/`, имя содержит номер), решения — `decisions.md` (grep по ключу).

- **с1** (2026-08-20) постановка харнесса: `CLAUDE.md`/`AGENTS.md`, протокол сессий,
  шесть скиллов, три субагента, библия 01–06 по коду, снос врущих правил Cursor.
  Разбор кода восемью агентами нашёл семь дыр «клиенту не верим», отсутствие
  ErrorBoundary и отсутствие DDL в репозитории.
- **До с1** — история без хэндофов, восстанавливается только по git-логу: Vue-версия,
  переписывание на React, аудит интерфейса и безопасности 19.08.2026.
