# Current State — ARGO Warehouse

**Сессия:** 4 (2026-08-20) — закрыта
**Актуальный handoff:** `docs/handoffs/2026-08-20-4-cleanup-audit.md`
**Ветка:** `session-4-cleanup` — **влита в `main` и выкачена.**
**Прод совпадает с репозиторием:** `a7b96b8`, `warehouse.argomedia.uz` отвечает 200.
**Последний doc-audit:** не проводился; `03-data-model` сведён с живой базой в с2.
Следующий рубеж — ≈с6.
**Аудит архитектуры:** проведён в с4 —
`docs/project/audits/2026-08-20-architecture.md`. 66 находок пережили адверсариальную
проверку (1 HIGH, 7 medium), 5 отклонено. Вердикт: **проект собран правильно**;
системная проблема одна — у контрактов нет владельцев.
**Мост:** **подключён полностью.** Supabase — `warehousev4` (`umbqiktyqlfqqgupxqdm`),
Vercel — `warehousev4` (`prj_2Xh9KlIRla8fZaF4n8ZcSWDQ14MY`, команда
`team_VVVqjT57YkCiqVBzOXs0OBfs`).

Сводка «как есть сейчас», НЕ журнал. Хроника сессий — в `docs/handoffs/`,
«почему решили X» — `docs/project/decisions.md` (grep по ключу), «как устроено» —
библия `docs/project/01–06`.

## Курс

**ARGO Warehouse** — внутренняя система учёта AV-оборудования ARGO Media.
Сотрудник открывает систему, собирает список оборудования на мероприятие, сохраняет
его и выгружает Excel — и едет на выезд. Всё остальное подчинено этому сценарию.

- **Репозиторий:** `github.com/strix-kim/warehousev4`, ветка `main`. **Публичный** —
  README и всё в `public/` видны любому. **263 файла** после чистки с4 (было 440).
- **Весь живой код — `src/react`: 23 файла, 5 083 строки.** Легаси-Vue (173 файла,
  37 636 строк) удалён из дерева в с4 и живёт только в git-истории.
- **Стек:** React 19 + TypeScript 5.9 + Vite 7 + Supabase (Postgres/Auth/Data API),
  хостинг Vercel. Точные версии — `docs/system/tooling.md`.
- **Рабочая копия поднята (с3).** `npm install` сделан (91 пакет, 0 уязвимостей),
  `.env` собран из публичных значений через Supabase MCP, оба гейта зелёные,
  бандл 358.76 КБ (110.29 gzip). **Локальный запуск: `npm run dev` →
  `http://localhost:5180/`** (порт закреплён `--strictPort`, разбор — `tooling.md`).
  Чего по-прежнему нет: `.vercel/`, `supabase/config.toml`.
- **Путь проекта сменился (с3):** теперь
  `/Users/strix/Documents/Projects/warehouse.argomedia.uz` — целиком ASCII.
  Прежний каталог содержал неразрывный пробел `U+00A0`, из-за чего `cd` падал,
  а запись по абсолютному пути уходила в дерево-двойник. На старом месте оставлен
  симлинк, оба адреса рабочие. Разбор — `gotchas.md` §12.
- **Данные прода на 2026-08-20:** `equipment` — 1481, `equipment_movements` — 1391,
  `events` — 4, `mount_points` — 9, `public.users` — 1, `auth.users` — 1.
  Списки, позиции списков, история этапов и отчёты — **пусто**. То есть сохранённых
  списков в проде ещё нет, а аккаунт в системе ровно один.
- **Продукт переписан с Vue на React.** Vue-версия выведена из продукта целиком:
  события, точки монтажа, отчёты, роли, UI Kit в новый продукт НЕ входят.

## Каноны (нарушать нельзя, не переспросив)

- **База авторитетна.** Права, видимость, целостность и уникальность живут в Postgres.
  Правило, существующее только в React, не существует.
- **`main` — это прод (ФАКТ, проверен в с2), но осторожный режим СНЯТ (с3).** Все 20
  последних деплоев Vercel: `target: production`, `githubCommitRef: main`. Домены —
  `warehouse.argomedia.uz`, `warehousev4.vercel.app` и branch-алиас `main`.
  Решение прораба в с3: **слияние в `main` отдельной команды не требует.** Основание
  продуктовое — внутренняя система на несколько сотрудников, внешних пользователей нет,
  откат стоит `git revert`. Осталось одно условие, и оно про содержимое: **сливаем
  принятое** — подтверждённый чекпоинт с зелёными `npm run check` и `npm run build`.
  Работа по-прежнему идёт в ветке (это дёшево и даёт превью-деплой).
- **Схема — второй источник правды.** Любое изменение фиксируется миграцией в
  `supabase/migrations/`, даже если применено кликами в дашборде. Стартовая точка —
  `00000000000000_baseline_remote_schema.sql` (снимок прода на 2026-08-20, 134 объекта).
  Он не обновляется сам: правка схемы мимо миграции делает его враньём.
  `scripts/*.sql` — **не архив**: четыре файла от 19.08 применены к проду и
  зарегистрированы как миграции (порядок `scripts/` → `migrations/`, см. `03-data-model` §2).
  Новые SQL туда всё равно не кладём.
- **Дерево чистое: всё, что лежит в `src/`, работает.** Легаси-Vue (173 файла,
  37 636 строк) удалён в с4 — `src/` это ровно `src/react/`. Мусора в дереве
  не заводим: файл, переставший использоваться, удаляется тем же шагом. Образец
  из Vue-версии берётся из git-истории (`git show 4dd010c:src/pages/<файл>.vue`),
  в дерево не возвращается.
- **Прод открыт, необратимое объявляется (с3).** Технические барьеры сняты решением
  прораба: слияние в `main`, `execute_sql` и `apply_migration` разрешены без
  подтверждения. Взамен действует обязанность предупреждать: `DELETE`/`UPDATE`/`DROP`/
  `TRUNCATE` по живым данным агент показывает текстом ДО исполнения, с оценкой числа
  строк. `SELECT` и добавляющие миграции — свободно. Под подтверждением остались только
  платные и внешние действия (ветки Supabase, создание проектов, Edge Functions,
  `deploy_to_vercel`).
- **`revoke all on public.users` держится на `security definer`.** Авторизация цела
  ИМЕННО потому, что `private.is_app_member()` и `private.has_any_role()` объявлены
  `security definer`. «Починка» revoke уронит вход всем — разбор в `03-data-model`.
- **Тексты интерфейса — только через `tr(ru, uz)`.** Это не словарь, а функция:
  вызвал — обязан дать оба языка.
- **Прогон под двумя аккаунтами обязателен** для всего, что касается прав, ролей и
  чужих записей. Второй ПРОФИЛЬ браузера, не вторая вкладка: сессия живёт в
  `localStorage` и вкладки делят аккаунт. **Сейчас он НЕВЫПОЛНИМ:** в проде ровно
  один пользователь, второго аккаунта не существует. Пока он не заведён,
  `/rls-verify` не запускается, а с ним заблокированы все задачи про права.

## Работает

Механики поимённо — `02-features`, схема и права — `03-data-model`, провода —
`04-architecture`, вид — `06-design-system`. Здесь только то, о чём легко забыть.

### Продукт

- **Каталог оборудования:** поиск с дебаунсом, фильтры, постраничная загрузка,
  префетч следующей страницы, превью-drawer по клику на строку. Клик по строке
  открывает превью, добавление — отдельной кнопкой (сделано по фидбеку аудита:
  случайное нажатие меняло список).
- **Создание оборудования** работает; серийные и количественные позиции различаются.
- **Редактор списка:** два режима — «по единицам» (`specific`, серийные) и
  «по количеству» (абстрактный). Смешанный список сохраняет реальные `equipment_id`,
  а не превращается в абстрактный план.
- **Жизненный цикл списка** `draft → confirmed → issued → returned` с историей этапов
  и историей движения. **Переходы проверяются СЕРВЕРОМ** — клиентские гварды здесь
  дублируют серверные, а не подменяют их.
- **Экспорт в Excel** — самописный ZIP (метод store, ручной CRC32) и OOXML строками,
  без внешних библиотек.
- **Два языка** ru / uz, переключатель в сайдбаре, язык в `localStorage`.
- **Вход** через Supabase Auth; все шесть маршрутов за гейтом сессии.

### Инфраструктура

- **Кэш `persistentCache`** с TTL: страницы оборудования и списков — 10 минут,
  справочник категорий — 24 часа; после записи соответствующий кэш сбрасывается.
  При сетевой ошибке отдаёт старое значение — см. долги, это же и риск.
- **Ленивая загрузка всех шести маршрутов** + три отложенных прогревочных таймера
  в `AppShell`. Основной бандл **358.76 КБ (110.29 gzip)**, замер с4; собственного
  кода в нём 9,2 КБ — остальное React и Supabase, вендор не отделён.
- **RLS включён** на рабочих таблицах; индексы под внешние ключи добавлены,
  `search_path` у четырёх trigger-функций зафиксирован (аудит 19.08).
- **`vercel.json`:** immutable-кэш на год для `/assets/` и `/equipment-images/`,
  неделя для `/brand/` и `/illustrations/`, security-заголовки, SPA-рерайт.
- **Фото оборудования:** 176 WebP из 217 моделей; для 41 — категорийная пиктограмма.
  Контракт именования задаёт `EquipmentVisual`.

### Харнесс (заведён в с1)

- `CLAUDE.md` + парное зеркало `AGENTS.md` (его же читает Cursor вместо снесённых
  `.cursorrules`).
- `docs/system/workflow.md` — протокол сессий. **Общий для всех проектов прораба:**
  ритуалы, шаги и шаблон хэндофа совпадают, расходятся только «Гейты стека» и «Мост».
- Шесть скиллов: `session-start`, `checkpoint`, `session-close`, `doc-audit`,
  `rls-verify`, `deploy-verify`.
- Три субагента: `planner` (Fable/xhigh), `coder` (Opus/high), `scout` (Sonnet/medium).
- `docs/system/tooling.md` (версии, команды, чего НЕТ) и `docs/system/gotchas.md`
  (справочник граблей с триггерной картой, засеян десятью разделами).
- Библия `docs/project/01–06` написана по коду с проверкой каждого якоря.

## В процессе / долги

Полный список — `backlog.md`. Здесь то, что меняет планирование.

- **HIGH (аудит с4): повторное «Сохранить» на `/lists/new` создаёт ВТОРОЙ список.**
  `persistList` (`ListEditorPage.tsx:377`) ветвится по `listId` из `useParams`, а
  маршрут объявлен без параметра (`App.tsx:33`) — там он пуст всегда. `savedId`
  гасится при любой правке. Сохранил → поправил букву → сохранил = два документа.
  Удаление прибито к одной почте, сотрудник дубли не уберёт. **Спит только потому,
  что списков в проде ноль** — сработает на первом реальном выезде.
  `02-features.md:499` при этом называет сохранение «идемпотентным».

- **`baseline.sql` ни разу не исполнялся.** Полнота доказана машинной сверкой имён
  (134 из 134), синтаксис и идемпотентность — нет: прогона на чистой базе не было.
  Файл, который не запускается, даёт ложное чувство, что схема восстановима.
- **Инструмента применения SQL в репозитории нет:** ни `supabase/config.toml`, ни
  пакета `supabase`, ни CI. Миграции гоняются вручную — через дашборд или MCP.
- **Пять расхождений схемы с репозиторием**, найденных выпиской с2: `serialnumber`
  объявлен `not null` против nullable в TS · `public.users.id` не связан с `auth.users`
  внешним ключом · удаление списков захардкожено на почтовый адрес вместо роли ·
  дубль FK и четыре триггера пересчёта на `mount_points` · GIN-индекс полнотекстового
  поиска не используется ни одним запросом. Разбор с якорями — `03-data-model`,
  раздел «Расхождения схемы с репозиторием».
- **Отказоустойчивости нет.** ErrorBoundary в проекте отсутствует, все страницы —
  через `React.lazy`, `RouteBoundary` это только `Suspense` без catch. Выкатка нового
  билда при открытой вкладке → старые чанки исчезли → SPA-рерайт отдаёт HTML вместо
  JS → **белый экран всего приложения**. Плюс: ни одного `console.*` во всём
  `src/react`, сборщика ошибок нет — отказ у пользователя невидим. Плюс: офлайн не
  отслеживается, а кэш молча отдаёт старое.
- **Legacy-фолбэк списков срабатывает на ЛЮБОЙ ошибке**, без фильтра по коду (соседние
  функции гейтятся по `PGRST202/42883/PGRST205/42P01`). Сетевой сбой или 401 → все
  списки становятся «черновиками», выданные прячутся, **и это кэшируется на 10 минут.**
  Две копии кода.
- **Шесть дыр «клиенту не верим»** — перечень с якорями в `02-features`, раздел
  «Где клиент решает за базу». Самая дорогая: сервер пишет brand/model дословно из
  клиентского JSON, а дефицит считается джойном склада по этому тексту. Самая
  коварная: проверка уникальности серийного номера обёрнута в `try/catch`, где
  `catch` возвращает «дубля нет» — она отваливается при любом сбое запроса, а не
  только в гонке. Отдельно зафиксировано, что переходы статусов списка — НЕ дыра:
  клиентские гварды там дублируются серверными проверками.
- **Автоматических гейтов только два** — `tsc -b` и `vite build`. Линтера, тестов
  и CI нет; `.eslintrc.json` и `vitest.config.js` снесены в с1 как конфиги-призраки
  от Vue-стека. `.prettierrc` НЕ призрак — его настройки совпадают с фактическим
  стилем кода, это действующая конвенция без гейта.
- **Расхождение Node:** `engines.node` = 22.x, на машине 24.18.0. Локальная сборка
  не доказывает прод-сборку.
- **Безопасность прода не готова к внешнему запуску:** защита от скомпрометированных
  паролей отключена, MFA нестрогая, пароль из шести нулей, доступно обновление
  Postgres с исправлениями безопасности. Годится для закрытого теста, не для публики.
- **11 повторяющихся нормализованных серийников** — автоматически не чинятся,
  нужна физическая сверка наклеек.
- **Доступность:** фокус-ловушки в drawer'ах нет, фокус не возвращается на источник;
  `AppSelect` объявляет контракт listbox, но не выполняет его; `aria-live` нет нигде,
  кроме логина.
- **`styles.css` 889 строк** — единственный файл за порогом ~800. Аудит с4 добавил
  условие: перед распилом снести ~25 строк мёртвого CSS и схлопнуть «Readability
  pass» (`:800`, `:868`), который держится только порядком строк, — иначе распил
  молча вернёт старую типографику.
- **Судьба README и AUDIT решена в с4.** README переписан по фактам с проверкой каждого
  утверждения; AUDIT переехал в `docs/handoffs/2026-08-19-0-audit.md` как хроника
  сессии 0, а семь продуктовых идей из него, не записанных больше нигде, — в раздел
  «Продуктовая очередь» файла `05-roadmap.md`. Построчной сверки самого AUDIT
  с кодом по-прежнему не было и уже не будет: он объявлен архивом, а не источником.
- **Не прочитано полностью:** `xlsxExport.ts` (экранирование пользовательского текста
  в XML), тело RPC `create_equipment_list_document`, DML-часть большого SQL-скрипта
  (идемпотентность повторного прогона).

## Очередь целей (топ-3)

Прораб объявит задачу следующей сессии сам — очередь ниже: предложение агента.
Обоснование порядка — цена отказа, не бизнес-ценность (`decisions.md`, с1).
**Порядок пересобран после аудита с4**, полное обоснование каждого шага и ещё три
шага за пределами топ-3 — в отчёте, раздел «Порядок работ».

1. **Обезвредить то, что выстрелит первым** (шаг 1 отчёта): HIGH с дублем списка
   (`navigate` после создания + правка ложного «идемпотентно» в `02-features.md:499`
   тем же коммитом) · отказ NOT NULL при очистке «Локации» · показ числа единиц из
   ответа RPC вместо клиентского счёта · счётчик смешанного списка. Всё в двух файлах,
   одна сессия. **Почему первым:** единственный HIGH срабатывает на первом реальном
   использовании главного сценария продукта.
2. **Типы базы** (шаг 2 отчёта): `generate_typescript_types` →
   `createClient<Database>` → доменные типы вывести из схемы, убрать четыре приведения.
   **Почему вторым:** превращает целый класс ошибок в ошибки компиляции ДО того, как
   начнутся распилы и новые фичи. Закрывает заодно расхождение типов со схемой,
   висящее с с2.
3. **Приложение перестаёт падать молча** (веха 2, была №1 и перебита прорабом в с4,
   не отменена): граница ошибок, обработка провала чанка после выкатки, индикатор
   офлайна и возраста кэша, починка legacy-фолбэка списков в обеих копиях. Аудит
   подтвердил механизм живым запросом: пропавший чанк отдаётся как HTML с
   `immutable` на год.

Одно маленькое дело вне очереди, разблокирующее целый класс задач: **завести второй
аккаунт в проде** — без него `/rls-verify` мёртв, а с ним заблокировано всё про права.
Висит с с2. Аудит добавил ему цены: дыры кэша (переживает выход из аккаунта, ключ без
id пользователя) открываются ровно в день появления второго аккаунта.

Второе, на пять минут и не сделанное третью сессию подряд: **прораб открывает
приложение сам.** В с4 агент прогнал его живьём и принёс скриншоты, но человеческого
клика по-прежнему не было. Живой прогон проверяет, что код работает, а не что
продуктом удобно пользоваться.

Ниже топ-3, не отменено: **дыры «клиенту не верим»** (веха 3) · **сведение контрактов
к одному владельцу** (шаг 3 отчёта) · **кэш** (шаг 4) · **конфигурация: `npm ci`,
`manualChunks`, sourcemaps, гейт env** (шаг 5) · **распилы `styles.css` и
`EquipmentPage`** (шаг 6, только после шага 3) · **`baseline.sql` не исполнялся** ·
**безопасность Auth перед внешним запуском** · **линтер, тесты и CI** · **41 модель
без фотографии** · **доступность drawer'ов** · **тест с сотрудниками на реальном
выезде** · **семь продуктовых идей** в `05-roadmap.md`, раздел «Продуктовая очередь».

## Хроника вех

Указатель, а не пересказ: подробности каждой сессии — в её handoff'е
(`docs/handoffs/`, имя содержит номер), решения — `decisions.md` (grep по ключу).

- **с4** (2026-08-20) дерево вычищено и проаудировано. Легаси-Vue удалён целиком —
  177 файлов и 37 766 строк, репозиторий 440 → 263, бандл не сдвинулся ни на байт.
  Правило 7 переписано в восьми местах из «мёртвая зона» в «дерево чистое».
  Решена судьба README (переписан по фактам) и AUDIT (переехал в хронику сессии 0,
  семь уникальных продуктовых идей спасены в `05-roadmap`). Найдено и закрыто:
  прод раздавал бланк с банковскими реквизитами компании без авторизации. Аудит
  архитектуры 15 агентами: 66 находок, вердикт «собран правильно», одна системная
  проблема — у контрактов нет владельцев. Приложение впервые проверено живым
  прогоном: каталог на 1481 позицию, поиск, превью, пагинация, ноль ошибок консоли.
- **с3** (2026-08-20) окружение поднято: проект переехал на ASCII-путь (в старом имени
  каталога был неразрывный пробел `U+00A0`, ломавший `cd` и запись файлов),
  `npm install` + `.env` из публичных значений, локальный запуск на закреплённом 5180.
  Осторожный режим слияния в `main` снят решением прораба, правило переписано в двух
  файлах правил, `workflow.md` и четырёх скиллах. Прод догнал репозиторий: четыре
  коммита с1–с3 выкачены одним махом. Продуктового кода не тронуто.
- **с2** (2026-08-20) прод-схема зафиксирована в git: baseline-миграция на 134 объекта,
  снятая через MCP и сверенная машинно (134/134); `03-data-model` переписан из
  реконструкции в выписку — все 11 пунктов «не установлено» закрыты. Мост подключён,
  допущение «`main` — это прод» подтверждено фактом. Найдено пять расхождений схемы
  с репозиторием. Кода не тронуто ни строкой.
- **с1** (2026-08-20) постановка харнесса: `CLAUDE.md`/`AGENTS.md`, протокол сессий,
  шесть скиллов, три субагента, библия 01–06 по коду, снос врущих правил Cursor.
  Разбор кода восемью агентами нашёл семь дыр «клиенту не верим», отсутствие
  ErrorBoundary и отсутствие DDL в репозитории.
- **До с1** — история без хэндофов, восстанавливается только по git-логу: Vue-версия,
  переписывание на React, аудит интерфейса и безопасности 19.08.2026.
