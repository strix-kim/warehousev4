# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
outputs
*.local
*.tsbuildinfo
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Служебные worktree харнесса (создаются агентами Claude Code)
.claude/worktrees/

# Playwright MCP: профиль браузера (в нём живёт сессия прораба) и выгрузки
# снимков/PDF. Профиль держим на диске намеренно — иначе логин пропадает
# между запусками и агент не может дойти дальше формы входа.
.playwright-profile/
.playwright-out/
